const ErrorState = () => {
  return (
    <div className="rounded-lg bg-error/10 text-error p-3 text-sm">
      Failed to load tasks. Please try again.
    </div>
  );
};

export default ErrorState;
